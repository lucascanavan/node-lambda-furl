{
  "name": "node-lambda-furl",
  "version": "1.0.0",
  "description": "Sample AWS Lambda using Function URL, nodejs, typescript & terraform.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "deploy": "./deploy.sh",
    "destroy": "cd ./terraform && terraform destroy -auto-approve"
  },
  "author": "Lucas Canavan",
  "dependencies": {
    "lambda-api": "^0.11.2"
  },
  "devDependencies": {
    "aws-lambda": "^1.0.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
